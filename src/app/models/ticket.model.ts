export interface Ticket {
  ticketId: number;
  id: number;
  eventId: number;
  userId: number;
  purchaseDate: string;
  eventTitle: string;
  eventDate: string;
  seatNumber: string;
  status: string;
}